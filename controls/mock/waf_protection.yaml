id: mock.waf_protection
name: WAF Protection Composite
description: Composite control verifying WAF protection through multiple sources
threat_mitigated: Unfiltered web traffic reaching application servers
observers:
  - module_id: mock.test
  - module_id: mock.network
testers:
  - module_id: mock.safety_test
evaluation:
  cel: "effective_count > 0 && ineffective_count == 0"
