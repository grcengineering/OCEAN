[approved]
cargo = ["anyhow", "axum", "base64", "cel-interpreter", "chrono", "clap", "cron", "crossterm", "handlebars", "hex", "hmac", "jsonschema", "ratatui", "regex", "rusqlite", "secrecy", "serde", "serde_json", "serde_yaml", "sha2", "tempfile", "thiserror", "tokio", "tower", "tracing", "tracing-subscriber", "ureq", "url", "uuid", "grc-controls-apis", "grc-controls-models"]
# sscsb approved-packages baseline.
#
# A dependency not in this baseline (and not already in the previous manifest
# revision) blocks at commit time until a human approves it:
#   sscsb deps check              # validate existence + typosquat heuristics
#   sscsb deps approve <eco>:<name>
#   sscsb deps baseline           # approve everything currently in manifests
#
# [approved]
# cargo = ["serde"]
# npm = []
# pypi = []
# go = []
# rubygems = []
